---
description: 新会话或新问题时先做项目 bootstrap，先读短文档再行动
alwaysApply: true
---

# Session Bootstrap

- 出现以下任一情况时，先执行 bootstrap，再开始搜索、编码、调试或长推理：
- 新对话开始
- 用户提出新的目标、子系统或问题域
- 从实现/排障切到架构/文档/Git 流程
- 当前上下文已经很长，任务边界不清

## Bootstrap 顺序

1. 读 `.cursor/project/current-focus.md`
2. 读 `.cursor/project/status.md`
3. 读 `.cursor/project/open-issues.md`
4. 若任务是 `AP_HAL_RTT` 对齐 `ChibiOS` 的专项任务，再补读 `.cursor/alignment-status.md` 与 `.cursor/alignment-issues.md`；这两者是专项档案，不替代 `project/*.md` 主入口
5. 任务若涉及命令、调试、烧录、验证，再读 `.cursor/project/command-catalog.md` 与相关 Skill
6. 任务若属于外设、总线、驱动或子系统验证，再读 `.cursor/project/driver-validation-matrix.md`、`docs/AP_HAL_RTT_DRIVER_VALIDATION.md` 或对应 driver-validation Skill
7. 任务若涉及设计取舍、结构整理、里程碑或 Git 流程，再读 `.cursor/project/decision-log.md` 与 `.cursor/project/milestone-plan.md`
8. 仅在续接具体排障时，补读 `.cursor/agent-trace.md` 最近 20-50 行，不全文重读

## 启动摘要

- bootstrap 完成后，先用 3-6 句说明：
- 当前阶段
- 当前主线目标
- 当前问题属于哪一层
- 本次读取了哪些文档
- 下一步先做什么

## 禁止

- 在 bootstrap 前直接进入大范围搜索或修改
- 默认全文重读长 trace 或长聊天导出
- 把上一个问题的假设直接带入当前新问题
- bootstrap 首轮默认不读超过 3 个 project 文档；只有在任务类型明确要求时才继续补读专项文档
