---
description: AP_HAL_RTT 项目主线目标与优先级，防止被局部细节带偏
alwaysApply: true
---

# AP_HAL_RTT Delivery Goals

- 优先级始终按以下顺序收敛：
- bring-up：bootloader -> app -> scheduler -> main
- 通信：UART/USB CDC -> MAVLink -> 参数
- 传感器：SPI/I2C probe -> IMU/Baro 数据有效
- 运行完整性：存储、PWM、RCInput、AnalogIn、回归
- 架构清理：HAL/BSP/hwdef 分层、硬编码下沉、数据驱动化
- 多板化：新板模板、board matrix、迁移路径

## 处理原则

- 若问题不阻断当前里程碑，不要无限深挖
- 若某现象更像环境问题，先明确隔离边界，再决定是否继续改代码
- 架构整理不得破坏当前 `CUAV v5` 可运行基线
- 默认优先对齐 `AP_HAL_ChibiOS` 的语义与职责边界，而不是复制板级硬编码
