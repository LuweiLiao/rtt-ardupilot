---
description: 将 EmbodiSkill / SkillEvolver 的技能自进化思想转化为本仓库代理工作流程
alwaysApply: true
---

# 技能自进化工作法

本规则把 EmbodiSkill 与 SkillEvolver 的共同思想落到本仓库：技能、规则、脚本和调试流程都视为可版本化、可验证、可审计的行为制品，而不是一次性提示词。

## 适用场景

- 复现失败、测试失败、硬件验证失败、子代理结果矛盾时
- 新增或修改 `.cursor/rules/`、`.cursor/skills/`、`.claude/skills/`、测试脚本、调试脚本时
- 从某次排障中提炼出可复用经验时

## 循环

1. **部署后观察**：不要只凭作者视角判断规则/技能/脚本有效；必须看它在真实任务、测试、子代理或 fresh context 中是否被实际触发和使用。
2. **区分失败类型**：失败先分为三类：制品内容错误、制品有效但执行方未遵守、任务/环境证据不足。不要把“执行失误”误改成错误规则。
3. **局部补丁优先**：优先对现有规则/脚本做最小可解释补丁，保留已验证有效的指导；只有结构失控时才重写。
4. **对比成功/失败轨迹**：从通过与失败的命令输出、trace、子代理报告中提取差异；新增规则必须能指向具体证据，避免凭印象泛化。
5. **独立审计**：沉淀为稳定规则前，自查是否存在训练样例硬编码、过度场景化、触发条件太窄、主动作埋得太深、或“看似正确但运行时不被调用”。

## 进入项目记忆

- 尚未闭环的经验只写入 `.cursor/agent-trace.md` 或 `open-issues.md`。
- 已被构建、测试、硬件现象或多次复现支撑的经验，才升级到 `status.md`、`command-catalog.md`、`decision-log.md` 或规则文件。
- 修改规则/技能后，下一次相关任务要观察它是否真正改变行为；若没有触发，优先修描述、触发条件或起手步骤，而不是继续增加正文。
