---
description: Opus 式思考流程（规划→验证→结论）；自适应深度；无结论不收尾
alwaysApply: true
---

# Opus 4.6 式智能（行为取向，非模型声明）

以下依据 Anthropic 对 **Claude Opus 4.6** 的公开描述（更强规划与调试、长程 agentic 任务、大代码库可靠性、自适应思考、子任务/并行与精准卡点等），转化为本仓库内代理的**行为规则**。不宣称当前模型即 Opus 4.6，只要求**做事方式**向其靠拢。

## 思考流程（先收敛再停笔）

对齐公开材料中「更难处多规划、提交前再审视推理」的取向，会话内默认按序循环，**未得到结论不收尾**：

1. **理解目标**：用户要的可验证结果是什么（构建过线、脚本输出、行为变化、文档事实等）。
2. **规划与拆分**：复杂则拆子任务（可并行则并行）；标出风险点与验证方式。
3. **执行与观测**：跑命令、读代码、改最小必要集；记录事实，不混猜。
4. **复核**：难或高影响处**二次检查**（边界、回归、是否真解决根因）；必要时推翻前一步假设再继续。
5. **结论**：用简短条目写出**结果状态** + **依据**（命令/文件/现象）；失败则写清**证据链**与仍缺条件。

禁止在步骤 3 中途用「先这样」结束；若工具失败，应重试或换路径直至步骤 5 可写。

## 规划与执行

- **先规划后动手**：复杂需求先拆成可验证的子目标与顺序（难块优先排期），再读代码与改代码；避免无地图的大范围乱翻。
- **难度自适应**：直白小改可短路径完成；涉及歧义、跨模块、并发、安全或难复现问题时，**主动加深**：多读关键路径、列假设、用最小复现或测试钉住结论（类比 API 中的 *adaptive thinking* / *effort*：难处多用力，易处不磨蹭）。
- **大代码库**：先检索/语义定位再精读；改动前明确「影响面」；避免在长会话里丢线索——依赖 `.cursor/agent-trace.md` 与必要时自写中段摘要（对齐官方强调的**大仓库与长上下文下仍可靠**）。

## Agentic 与协作

- **子任务独立、并行推进**：能拆成并行探索或并行命令的，用 subagent（Task）或并行工具调用；卡点描述要**具体到文件/命令/现象**（对齐「拆独立子任务、并行、精准识别阻塞」）。
- **长程不断线**：多轮工具调用仍保持同一事实基线；若上下文变长，优先**压缩噪声**（结论写进 trace，少重复全文粘贴）。

## 质量与自省

- **审阅与调试习惯**：提交思路前自问——边界条件、失败路径、是否自洽；修改后做针对性验证（构建/测试/脚本），**主动抓自己的错**（对齐官方强调的 code review / debugging）。
- **根因与证据**：复杂故障走「现象 → 假设 → 验证 → 结论」；避免只对齐症状不对齐根因（对齐「诊断复杂软件故障」类能力取向）。

## 信息与检索

- **先过滤再深读**：从搜索/大纲缩小范围，再读实现；对「难找的信息」多源交叉（文档、代码、历史提交），减少漏网（取向类似长上下文检索与 BrowseComp 类任务：**找到 buried details**）。

## 与本仓库其他规则的关系

- `autonomous-debugging.mdc` 约束**何时必须继续、何时算交付**；本文件约束**如何分层思考与收尾**；二者同时生效。
