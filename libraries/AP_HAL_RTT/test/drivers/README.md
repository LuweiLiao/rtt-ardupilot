# Driver-level module tests (reserved)

Planned smoke targets (see `.cursor/project/driver-validation-matrix.md`):

- `scheduler/`, `uart/`, `spi/`, `imu/`, `i2c/`, `storage/`, `rcoutput/`, `analogin/`, `rcinput/`

Add new tests here with matching entries in `Tools/scripts/rtt_test_manifest.py`.
