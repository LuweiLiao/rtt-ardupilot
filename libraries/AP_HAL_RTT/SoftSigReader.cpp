/*
 * AP_HAL_RTT — SoftSigReader translation unit (matrix parity).
 *
 * Implementation is interrupt-only via SoftSigReaderInt; see SoftSigReader.h.
 * ChibiOS reference: libraries/AP_HAL_ChibiOS/SoftSigReader.cpp (ICU+DMA).
 */

#include "SoftSigReader.h"

/* Intentionally no additional code — API lives in header inlines. */
